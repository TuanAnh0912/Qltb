@using qltb.Data.ResVMs
@model ListWithPaginationResVM
@{
    ViewBag.Title = "Nguồn cấp";
    Layout = "~/Views/Shared/_Layout.cshtml";

    var SuccessNotification = TempData["SUCCESS"];
    var WarningNotification = TempData["WARNING"];

    // get ten tinh param
    string TenNguonCapQueryParam = "";
    Model.CurrentQueryParamsDict.TryGetValue("TenNguonCap", out TenNguonCapQueryParam);
}
<div class="modal fade" id="add-modal" tabindex="-1" role="dialog" aria-labelledby="addModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Thêm mới nguồn cấp</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form id="add-nguon-cap-form">
                    <div class="row">
                        <div class="col-12">
                            <div class="form-group">
                                <label>Mã nguồn cấp</label>
                                <input type="text" class="form-control" name="MaNguonCap" />
                            </div>
                            <div class="form-group">
                                <label>Tên nguồn cấp</label>
                                <input type="text" class="form-control" name="TenNguonCap" />
                            </div>
                        </div>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-outline-danger" data-dismiss="modal">Hủy</button>
                <button type="button" class="btn btn-primary" id="add-nguon-cap-btn">Thêm</button>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="edit-modal" tabindex="-1" role="dialog" aria-labelledby="editModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="editModalLabel"></h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form id="edit-nguon-cap-form">
                    <div class="row">
                        <div class="col-12">
                            <div class="form-group">
                                <label>Mã nguồn cấp</label>
                                <input type="text" class="form-control" name="MaNguonCap" />
                            </div>
                            <div class="form-group">
                                <label>Tên nguồn cấp</label>
                                <input type="text" class="form-control" name="TenNguonCap" />
                            </div>
                        </div>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-outline-danger" data-dismiss="modal">Hủy</button>
                <button type="button" class="btn btn-primary" id="update-nguon-cap-btn">Cập nhật</button>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="delete-modal" tabindex="-1" role="dialog" aria-labelledby="deleteModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Bạn có chắc chắn muốn xóa</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-outline-danger" data-dismiss="modal">Hủy</button>
                <button type="button" class="btn btn-primary" id="delete-nguon-cap-btn">Xóa</button>
            </div>
        </div>
    </div>
</div>
<div class="d-md-flex align-items-md-start mb-3">
    <h1 class="page-title mr-sm-auto"> Danh sách nguồn cấp</h1><!-- .btn-toolbar -->
    <div class="btn-toolbar">
        <a href="#" type="button" onclick="Add()" class="btn btn-subtle-primary"><i class="fal fa-plus mr-1"></i> <span class="ml-1">Thêm mới</span></a>
    </div><!-- /.btn-toolbar -->
</div>
<div class="page-section">
    <div class="row">
        @*<div class="col-12">
            <div class="card card-fluid">
                <div class="card-header">
                    <button onclick="Search()" class="btn btn-sm btn-outline-info">Tìm kiếm</button>
                    <div class="float-right">
                        <button class="btn btn-sm btn-primary" onclick="Add()">Thêm mới</button>
                    </div>
                </div>
                <div class="card-body">
                    <form id="search-nguon-cap-form">
                        <div class="row">
                            <div class="col-lg-3 col-md-4 col-sm-4 col-12">
                                <div class="form-group">
                                    <label>Tên nguồn cấp</label>
                                    <input class="form-control" type="text" name="TenNguonCap" placeholder="Tên nguồn cấp..." value="@TenNguonCapQueryParam" />
                                </div>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>*@

        <div class="col-12">
            <div class="card card-fluid">
                <div class="card-header">
                    <form id="search-nguon-cap-form">
                        <div class="row">
                            <div class="col-lg-3 col-md-4 col-sm-4 col-12">
                                <div class="form-group">
                                    <input class="form-control" type="text" name="TenNguonCap" placeholder="Tên nguồn cấp..." value="@TenNguonCapQueryParam" />
                                </div>
                            </div>
                            <div class="col-lg-3 col-md-4 col-sm-4 col-12">
                                <div class="form-group">
                                    <button onclick="Search()" class="btn btn-primary">Tìm kiếm</button>
                                </div>
                            </div>
                        </div>
                    </form>
                </div>
                <div class="card-body" style="overflow-x: auto">
                    <div class="table-responsive" style="min-width: 1000px">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th>STT</th>
                                    <th>Mã nguồn cấp</th>
                                    <th>Tên nguồn cấp</th>
                                    <th class="text-center"></th>
                                </tr>
                            </thead>
                            <tbody>
                                @{
                                    foreach (var nguonCapResVM in (List<NguonCapResVM>)Model.Objects)
                                    {
                                        <tr>
                                            <td>@nguonCapResVM.STT</td>
                                            <td><code>@nguonCapResVM.MaNguonCap</code></td>
                                            <td>@nguonCapResVM.TenNguonCap</td>
                                            <td>
                                                <button class="btn btn-sm btn-outline-info" onclick="Edit('@(nguonCapResVM.NguonCapId)')"><i class="fas fa-pencil"></i></button>
                                                <button class="btn btn-sm btn-outline-danger" onclick="Delete('@(nguonCapResVM.NguonCapId)')"><i class="fas fa-trash-alt"></i></button>
                                            </td>
                                        </tr>
                                    }
                                }
                            </tbody>
                        </table>
                        <ul class="pagination justify-content-center mt-4">
                            @{
                                foreach (var paginationResVM in (List<PaginationResVM>)Model.Paginations)
                                {
                                    <li class="page-item @(paginationResVM.IsActived.Value ? "active":  "") @(paginationResVM.IsDisabled.Value ? "disabled":  "")">
                                        <a class="page-link" href="/tu-dien/nguon-cap@(paginationResVM.Url)" tabindex="-1">@Html.Raw(paginationResVM.Text)</a>
                                    </li>
                                }
                            }
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section scripts{
    <script src="~/assets/javascript/hanhnd_1.js"></script>
    <script>
        function Add() {
            $('#add-modal').modal('show');
            $('#add-nguon-cap-form input[name=MaNguonCap]').val('');
            $('#add-nguon-cap-form input[name=TenNguonCap]').val('');
            $("#add-nguon-cap-btn").unbind();
            $("#add-nguon-cap-btn").click(function () {
                AddNguonCap();
            });
        }

        function Edit(nguonCapId) {
            $.ajax({
                url: "/tu-dien/thong-tin-nguon-cap",
                data: {
                    nguonCapId: nguonCapId
                },
                type: "get",
                success: function (response) {
                    if (response) {
                        setTimeout(function () {
                            $('#edit-modal').modal('show');
                            $('#editModalLabel').html(response.TenNguonCap)
                            $('#edit-nguon-cap-form input[name=MaNguonCap]').val(response.MaNguonCap);
                            $('#edit-nguon-cap-form input[name=TenNguonCap]').val(response.TenNguonCap);
                            $("#update-nguon-cap-btn").unbind();
                            $("#update-nguon-cap-btn").click(function () {
                                UpdateNguonCap(response.NguonCapId);
                            });
                        }, 500);
                    }
                    else {
                        setTimeout(function () { toastr["warning"]('Không tìm thấy dữ liệu') }, 500)
                    }

                },
                error: function (xhr) {
                }
            });
        }

        function Delete(nguonCapId) {
            $('#delete-modal').modal('show');
            $("#delete-nguon-cap-btn").unbind();
            $("#delete-nguon-cap-btn").click(function () {
                DeleteNguonCap(nguonCapId);
            });
        }

        function DeleteNguonCap(nguonCapId) {
            console.log(nguonCapId)
            $.ajax({
                url: "/tu-dien/xoa-thong-tin-nguon-cap",
                data: {
                    NguonCapId: nguonCapId
                },
                type: "post",
                success: function (response) {
                    location.reload();
                },
                error: function (xhr) {
                }
            });
        }

        function UpdateNguonCap(nguonCapId) {
            let data = {
                NguonCapId: nguonCapId
            }
            $('#edit-nguon-cap-form').serializeArray().forEach(ele => {
                data[ele.name] = ele.value
            })

            $.ajax({
                url: "/tu-dien/cap-nhat-thong-tin-nguon-cap",
                data: data,
                type: "post",
                success: function (response) {
                    location.reload();
                },
                error: function (xhr) {
                }
            });
        }

        function AddNguonCap() {
            let data = {}
            $('#add-nguon-cap-form').serializeArray().forEach(ele => {
                data[ele.name] = ele.value
            })

            $.ajax({
                url: "/tu-dien/them-thong-tin-nguon-cap",
                data: data,
                type: "post",
                success: function (response) {
                    location.reload();
                },
                error: function (xhr) {
                }
            });
        }

        function Search() {
            let queryParams = $('#search-nguon-cap-form').serialize();
            console.log(queryParams);
            location.replace("/tu-dien/nguon-cap?" + queryParams);
        }

        $(document).ready(function () {
            if ('@(SuccessNotification)') {
                toastr["success"]('@(SuccessNotification)')
            }
            else if ('@(WarningNotification)') {
                toastr["warning"]('@(WarningNotification)')
            }

            $("#search-nguon-cap-form").submit(function (e) {
                e.preventDefault();
            })
        })
    </script>
}
